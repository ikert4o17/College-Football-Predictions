name: Test 2026 Preseason SP+ Anchor

on:
  workflow_dispatch:
  push:
    paths:
      - "ratings/preseason_model_v4_2026_sp_anchor.py"
      - "ratings/audit_2026_sp_anchor_v4_effect.py"
      - ".github/workflows/test_2026_sp_anchor.yml"

permissions:
  contents: write

jobs:
  test-anchor:
    runs-on: ubuntu-latest
    env:
      CFBD_API_KEY: ${{ secrets.CFBD_API_KEY }}

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          if [ -f requirements.txt ]; then pip install -r requirements.txt; fi

      - name: Verify CFBD secret
        run: |
          if [ -z "$CFBD_API_KEY" ]; then
            echo "CFBD_API_KEY repository secret is required."
            exit 1
          fi

      - name: Download 2026 SP+ ratings
        env:
          FORCE_CFBD_REFRESH: "1"
        run: python -m data.download_sp_ratings 2026 2026

      - name: Run same-season SP+ anchor plus full V4 experiment
        run: python -m ratings.preseason_model_v4_2026_sp_anchor

      - name: Audit incremental V4 effect
        run: python -m ratings.audit_2026_sp_anchor_v4_effect

      - name: Commit experiment outputs
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add data/raw/sp_ratings/2026.json \
                  data/processed/preseason_ratings_v4_2026_sp_anchor.json \
                  data/processed/preseason_2026_anchor_comparison.json \
                  data/processed/preseason_2026_sp_anchor_v4_effect.json
          if git diff --cached --quiet; then
            echo "No changes to commit."
          else
            git commit -m "Audit full V4 on 2026 preseason SP+ anchor"
            git push
          fi
